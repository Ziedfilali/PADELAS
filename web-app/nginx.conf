server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Avoid stale UI while developing (browser/service worker/proxy caches)
  add_header Cache-Control "no-store, max-age=0" always;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
