# Loaded by Expo during `npx expo start` (dev).
# Points the SPA at model-service — required because empty EXPO_PUBLIC_API_BASE is reserved
# for the Docker nginx build (same-origin /api proxy).
EXPO_PUBLIC_API_BASE=http://localhost:8000
